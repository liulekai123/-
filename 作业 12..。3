#include<stdio.h>
#include<math.h>
int a(int i);
int main(void)
{
    int i;
    for(i=2;i<=500;i++)
    {
        if(a(i)!=0)
        {
            printf("%d ",i);
        }
    }
    return 0;
}
int a(int i)
{
    for(int j=2;j<=sqrt(i);j++)
    {
        if(i%j==0) return 0;
    }
    return 1;
}
